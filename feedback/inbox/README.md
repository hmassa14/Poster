Drop feedback here as `.md` or `.txt` files (this README is ignored). Name a
file `publisher-*.md` to mark it as coming from the publisher, which outranks
reader feedback when they conflict. GitHub Issues labeled `feedback` are pulled
in automatically. Run `poster feedback` to fold everything into the style
guide, format spec, and lessons; processed items move to `feedback/processed/`.
